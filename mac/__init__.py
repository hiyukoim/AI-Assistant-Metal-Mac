# AI-Assistant Mac port — Python package marker.
# The mac/ directory holds Mac-only Python modules (and Bash scripts).
# This __init__.py makes `from mac.comfy_shim import register_shim`
# resolve when AI_Assistant.py is run from the repo root.
